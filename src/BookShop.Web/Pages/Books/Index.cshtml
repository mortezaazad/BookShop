@page
@model BookShop.Web.Pages.Books.IndexModel
@{
    ViewData["Title"] = "Books";
}

<h2>Book List</h2>

<p>
    <a asp-page="./Create" class="btn btn-primary">Add New Book</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var item in Model.BookList)
        {
            <tr>
                <th>@item.Name</th>
                <th>@item.Price</th>
                <th>
                    <a asp-page="./Edit?id=@item.Id">Edit</a>
                    <a asp-page="./Edit?id=@item.Id">Delete</a>
                </th>
            </tr>
        }*@
    </tbody>

</table>